GITHUB_TOKEN=github_pat_xxx
GITHUB_ORG=Flash-Lighting-Solutions
GITHUB_REPOS=pay-with-flash-web,pay-with-flash-server

# Leave blank to disable auth (fine for local dev).
# Set to a shared password to gate the whole app + /api with HTTP Basic auth.
DASHBOARD_PASSWORD=
